[project]
name = "deepseek-export"
version = "0.1.0"
description = "批量导出 DeepSeek Web Chat 对话为 Markdown，可选删除已导出对话"
requires-python = ">=3.10"
dependencies = [
    "requests>=2.28",
    "playwright>=1.40",
]
